abbr -g "art"="php artisan"
abbr -g "bc"="bin/console"
abbr -g "b"="brew"
abbr -g "brew s"="brew search"
abbr -g "brew i"="brew install"
abbr -g "c"="composer"
abbr -g "composer c"="composer config"
abbr -g "composer g"="composer global"
abbr -g "composer i"="composer install"
abbr -g "d"="docker"
abbr -g "dc"="docker compose"
abbr -g "docker compose c"="docker compose config"
abbr -g "docker compose d"="docker compose down"
abbr -g "docker compose e"="docker compose exec"
abbr -g "docker compose l"="docker compose logs"
abbr -g "docker compose ll"="docker compose logs -n 0 -f"
abbr -g "docker compose r"="docker compose restart"
abbr -g "docker compose u"="docker compose up -d"
abbr -g "docker e"="docker exec -it"
abbr -g "docker i"="docker image"
abbr -g "docker n"="docker network"
abbr -g "docker v"="docker volume"
abbr -g "fxy"="fx --yaml"
abbr -g "g"="git"
abbr -g "gd"="git dt HEAD"
abbr -g "gds"="git dt --staged"
abbr -g "gdu"="git diff -w HEAD"
abbr -g "git a"="git add"
abbr -g "git b"="git branch"
abbr -g "git c"="git config"
abbr -g "git ca"="git commit -a"
abbr -g "git cav"="git commit -av"
abbr -g "git ci"="git commit"
abbr -g "git civ"="git commit -v"
abbr -g "git co"="git checkout -m"
abbr -g "git cp"="git cherry-pick"
abbr -g "git d"="git diff -w"
abbr -g "git ds"="git diff -w --staged"
abbr -g "git dsu"="git -c core.pager='delta' diff -w --staged"
abbr -g "git du"="git -c core.pager='delta' diff -w"
abbr -g "git f"="git fetch"
abbr -g "git l"="git log"
abbr -g "git m"="git merge"
abbr -g "git pl"="git pull"
abbr -g "git ps"="git push"
abbr -g "git rl"="git reflog"
abbr -g "git rs"="git restore -SW"
abbr -g "git sh"="git show"
abbr -g "git ss"="git stash"
abbr -g "git ssa"="git stash apply"
abbr -g "git ssl"="git stash list"
abbr -g "git sss"="git stash show"
abbr -g "git st"="git status"
abbr -g "git sw"="git switch"
abbr -g "gpf"="git push --force"
abbr -g "gpuoh"="git push -u origin HEAD"
abbr -g "grh"="git reset --hard"
abbr -g "gs"="git status"
abbr -g "gsh"="git-difftool-show"
abbr -g "k"="kubectl"
abbr -g "kcm"="kitten @ create-marker iregex 1 \"%\""
abbr -g "kcmt"="kitten @ create-marker -m neighbor:top iregex 1 \"%\"; exit"
abbr -g "kssh"="kitten ssh"
abbr -g "kubectl c"="kubectl config"
abbr -g "kubectl e"="kubectl exec -it"
abbr -g "ld"="lazydocker"
abbr -g "lg"="lazygit"
abbr -g "nest g"="nest generate"
abbr -g "php artisan m"="php artisan migrate"
abbr -g "php artisan mr"="php artisan migrate:rollback"
abbr -g "rgp"="rg --engine pcre2"
abbr -g "t"="tofu"
abbr -g "tin"="php artisan tinker --execute \"%\""
abbr -g "whi"="while true; do %; sleep 3; done"
abbr -g "yayinfo"="yay -Qi"
abbr -g "yayinstall"="yay -S"
abbr -g "yaylist"="yay -Q"
abbr -g "yayrm"="yay -Rs"
abbr -g "yaysearch"="term=\"%\"; yay -Ss \"\$term\" | rg -A1 \"^\S.*\$term\""
abbr -g "yayup"="yay -Syu --noconfirm"
