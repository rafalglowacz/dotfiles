" Vim's arrow keys cause opening folds when moving over them. The native editor
" actions don't have that problem.
nmap i <Action>(EditorUp)
nmap j <Action>(EditorLeft)
nmap k <Action>(EditorDown)
nmap l <Action>(EditorRight)
